[package]
name = "quote_client"
version = "0.1.0"
edition = "2024"

[dependencies]
socket2 = "0.5"
clap = { version = "4.0", features = ["derive"] }
bincode = "1.3"
quote_generator_lib = { path = "../quote_generator_lib" }
chrono = "0.4"
